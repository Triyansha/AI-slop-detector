# Learn-from-use calibration

Append user corrections here. The skill reads this before scoring to adjust for known false positives / misses.

Format per entry:
- **Date · artifact · correction** — what was wrong (false positive / missed tell) and the takeaway.

_(empty — grows from use)_
